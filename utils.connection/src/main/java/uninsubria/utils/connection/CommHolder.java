package uninsubria.utils.connection;

/**
 * Interface that holds references of ports for communication between client and server.
 */
public interface CommHolder {
    int SERVER_PORT = 8888;
    int ROOM_PORT = 8889;
}
